apiVersion: v1
kind: Service
metadata:
  name: hospital-portal-service
  namespace: hospital-portal
  labels:
    app: hospital-portal
    project: 24CS2018-DevOps
spec:
  type: LoadBalancer
  selector:
    app: hospital-portal
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 5000
